# Template: Build + test + deploy (ECR push) on push to main
# Copy to: .github/workflows/main-deploy.yml
#
# Flow: build + test + artifact (ECR push, production environment) + tag.
# ECR repository is auto-created by the pipeline if missing.
#
# Required secrets:
#   AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION, AWS_ECR_URL

name: Main Build, Test & Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: write   # required by run_tag to push vX.Y.Z tag and create GitHub Release
  id-token: write

jobs:
  publish:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    uses: SOLDIFE/ci-templates/.github/workflows/contracts-main-pipeline.yml@main
    with:
      run_build: true
      run_test: true
      run_artifact: true
      push_latest: true
      environment: 'production'
      run_tag: true
      run_notifications: true
      notify_mention_on_failure: '@channel'
    secrets: inherit
