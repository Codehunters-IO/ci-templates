# Template: Deploy on release branch
# Copy to: .github/workflows/release-deploy.yml
#
# Triggers on push to release/* branches
# Runs: build + test + artifact + deploy (STAGING environment)
#
# Required secrets: Same as develop-deploy.yml
# Note: Semver is NOT run on release branches (version comes from branch name)

name: Deploy to STAGING

on:
  push:
    branches:
      - 'release/**'

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    uses: <org>/ci-templates/.github/workflows/java-main-pipeline.yml@main
    with:
      run_build: true
      run_test: true
      run_semver: false
      run_artifact: true
      run_deploy: true
      spring_profile: 'staging'
      health_check_path: '/actuator/health'
    secrets: inherit
